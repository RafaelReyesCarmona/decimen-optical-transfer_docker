version: '3.8'

services:
  app:
    build: .
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules # Evita conflictos con los node_modules de tu máquina local
    environment:
      - NODE_ENV=development
